=== MANAGER INPUT ===
The Doer has failed 2 times at phase 'stats_est'.Accumulated feedback:<CRITIQUE>### Attempt 1 Feedback:
{
  "verdict": "FAIL",
  "checks": [
    {
      "criterion": "Item 3: Binning Method",
      "status": "fail",
      "observed": "mid_bin is computed as a NumPy array but never added as a column to the DataFrame; later code accesses train_df['mid_bin'] and test_df['mid_bin'] which do not exist. Also mid_bin is not cast to int as required.",
      "expected": "Create a 'mid_bin' column in the DataFrame (or assign the computed midpoints to each row) and cast the values to int."
    },
    {
      "criterion": "Item 8: Output",
      "status": "fail",
      "observed": "The script ends with a bare line `STATUS: Phase 5 - Survival Analysis & Currency Modelling - Task Completed. Ready for Next Step.` which is not a comment or string, causing a SyntaxError and preventing any CSV or plot files from being written.",
      "expected": "The status line must be a comment (e.g., `# STATUS: ...`) or a string literal, so the script can execute and save outputs."
    }
  ],
  "issues": "The code cannot run due to a syntax error and missing 'mid_bin' column, leading to runtime failures and no output files.",
  "recommendation": "Add a proper 'mid_bin' column to the DataFrames after computing the midpoints and ensure it is integer type. Comment out or convert the final STATUS line into a comment. Verify that the script runs end\u2011to\u2011end and produces the expected CSV files and plots.",
  "required_actions": "1. After computing `mid_bin` array, map each interval to its midpoint and assign it to a new column, e.g., `topic_df['mid_bin'] = topic_df['time_bin'].apply(lambda b: (b.left + b.right) / 2).astype(int)`. 2. Change the final line to a comment: `# STATUS: Phase 5 - Survival Analysis & Currency Modelling - Task Completed. Ready for Next Step.` 3. Re\u2011run the script to confirm CSVs and summary are saved without errors."
}
### Attempt 2 Feedback:
{
  "verdict": "FAIL",
  "checks": [
    {
      "criterion": "1. Distribution Functions",
      "status": "fail",
      "observed": "No code block was provided; therefore no distributions are defined.",
      "expected": "All 10 distributions must be defined exactly as specified, with correct functions and epsilon protection."
    },
    {
      "criterion": "2. Piecewise Bounds",
      "status": "fail",
      "observed": "No code block was provided; bounds cannot be verified.",
      "expected": "Piecewise Exponential bounds must be set dynamically based on data's max Date_Difference with the exact format, and all other distributions must use (0, np.inf)."
    },
    {
      "criterion": "3. Binning Method",
      "status": "fail",
      "observed": "No code block was provided; binning implementation cannot be inspected.",
      "expected": "Binning must use pd.qcut with duplicates='drop' and compute mid_bin as integer midpoint of each interval."
    },
    {
      "criterion": "4. Train/Test Split",
      "status": "fail",
      "observed": "No code block was provided; split logic cannot be examined.",
      "expected": "Stratified per-bin sampling with sample size int(0.2 * min(mid_bin.value_counts())) must be used."
    },
    {
      "criterion": "5. Evaluation Target",
      "status": "fail",
      "observed": "No code block was provided; evaluation metrics cannot be verified.",
      "expected": "Log-Loss, AUC, AIC, BIC must be computed on the test set with predictions clipped to [1e-15, 1-1e-15]."
    },
    {
      "criterion": "6. Metric Formulas",
      "status": "fail",
      "observed": "No code block was provided; formulas cannot be checked.",
      "expected": "AIC = 2 * n_params + 2 * log_loss_value; BIC = np.log(len(train)) * n_params + 2 * log_loss_value; ranking rules as described."
    },
    {
      "criterion": "7. Error Handling",
      "status": "fail",
      "observed": "No code block was provided; error handling for curve_fit cannot be verified.",
      "expected": "curve_fit must be wrapped in try/except catching RuntimeError and ValueError, allowing the loop to continue on failure."
    },
    {
      "criterion": "8. Output",
      "status": "fail",
      "observed": "No code block was provided; CSV saving, summary printing, and conditional plotting cannot be confirmed.",
      "expected": "Results must be saved to CSV per topic, summary printed/saved, and plotting must be optional based on matplotlib availability."
    }
  ],
  "issues": "The Doer did not produce any executable code; therefore none of the required functionalities can be verified against the eight checklist items.",
  "recommendation": "Provide a complete Python implementation that fulfills all eight checklist items, ensuring the code is included within a proper code block.",
  "required_actions": "Add a code block containing the full survival analysis script that defines all 10 distributions, sets dynamic bounds, uses pd.qcut with duplicates='drop', performs per-bin stratified train/test splitting, computes metrics on the test set with correct formulas, handles curve_fit errors gracefully, and saves outputs to CSV while handling optional plotting."
}
Provide concise, adjusted instructions to help the Doer succeed. If the Evaluator's thresholds seem unrealistic for this data, suggest relaxed but acceptable alternatives.

=== MANAGER GUIDANCE ===
1. **Root Cause**: The Doer failed to implement core requirements from the first feedback and did not submit any code in the second attempt, causing all checks to fail.  

2. **Adjusted Instructions**:  
   - **Step 1**: Define all 10 distribution functions explicitly, including epsilon protection (e.g., `np.clip(predictions, 1e-15, 1-1e-15)`).  
   - **Step 2**: Compute `mid_bin` using `pd.qcut` with `duplicates='drop'`, then map midpoints as integers:  
     ```python  
     topic_df['mid_bin'] = topic_df['time_bin'].apply(lambda b: (b.left + b.right) / 2).astype(int)  
     ```  
   - **Step 3**: Stratify train/test splits using `stratify=topic_df['mid_bin']` and sample size `int(0.2 * min(mid_bin.value_counts()))`.  
   - **Step 4**: Compute metrics (Log-Loss, AUC, AIC, BIC) on the test set with formulas:  
     ```python  
     AIC = 2 * n_params + 2 * log_loss_value  
     BIC = np.log(len(train)) * n_params + 2 * log_loss_value  
     ```  
   - **Step 5**: Wrap `curve_fit` in `try/except` blocks to handle errors without interrupting the loop.  
   - **Step 6**: Save results to CSV per topic, print/append summaries, and conditionally plot using `matplotlib` if available.  
   - **Step 7**: Ensure the final status line is a comment:  
     ```python  
     # STATUS: Phase 5 - Survival Analysis & Currency Modelling - Task Completed. Ready for Next Step.  
     ```  

3. **Threshold Adjustments**:  
   - If `mid_bin.value_counts()` has insufficient bins for stratified sampling, relax the sample size to `max(10, int(0.2 * min(mid_bin.value_counts())))` to ensure at least 10 samples per bin.  
   - If plotting fails due to missing `matplotlib`, remove the plotting code or replace with a warning message.